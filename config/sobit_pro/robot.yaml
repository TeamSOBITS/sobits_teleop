/**:
  ros__parameters:
  
    robot_topic_name:
      joint_states_topic : joint_states
      # One controller drives every joint, so both groups map to it.
      joint_trajectory_topic:
        all: joint_trajectory_controller/joint_trajectory
        head: joint_trajectory_controller/joint_trajectory
        arm: joint_trajectory_controller/joint_trajectory
      cmd_vel_topic : cmd_vel